exports.parseCommand = async (text) => {
    // Placeholder: simulate parsing with dummy data
    return {
        intent: 'add_task',
        entities: {
            task: text
        },
        raw: text
    };
};
